# ベースイメージ（公式 Python）
FROM mcr.microsoft.com/devcontainers/python:3.11

# 必要なツール（追加で入れたい場合はここで）
# RUN apt-get update && apt-get install -y some-package

# 作業ディレクトリは自動で /workspaces/<folder-name> に設定されます
